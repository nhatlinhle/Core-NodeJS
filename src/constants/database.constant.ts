enum UserRole {
	ADMIN = 1,
	USER = 2,
}

enum UserStatus {
	ACTIVE = 1,
}

enum BlogStatus {
	ACTIVE = 1,
}

const CommentType = ['anime', 'blog'];

export { UserRole, UserStatus, BlogStatus, CommentType };
